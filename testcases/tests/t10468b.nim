##[

$`strutils.delete`

`strutils.delete`

]##

##[

## PRTEMP

PRTEMP2
-------

$`strutils.delete`

`strutils.delete`

]##

##[
see :`strutils.delete`

see :`delete`

see :`delete`, :`strutils.delete`
]##

##[
see `strutils.delete`_

see `strutils.delete`_, `strutils.delete`_.

see `strutils`_, `strutils`_.

see `strutils`_, `strutils`_, foo, `foo`, `bar`_.
]##
